# TamamHealth website — copy to .env.local for local development.

# The platform deployment that /login hands sign-ins over to. The website
# holds no session: it picks the portal and passes the username through as
# ?u=, and the platform does the actual authentication.
# Defaults to the public demo when unset.
NEXT_PUBLIC_PLATFORM_URL=http://localhost:3000
